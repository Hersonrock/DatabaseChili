#include <iostream>

int main()
{
        bool isRunning = true;
        while(isRunning){
                std::cout << "(l)load (s)ave (a)dd (q)uit (p)rint" 
                          << std::endl; 
        }

        return 0;
}
